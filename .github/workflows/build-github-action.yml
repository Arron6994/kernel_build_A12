name: Build Zeus Kernel with KernelSU and SusFS

on:
  push:
    branches: [ main, copilot/* ]
    paths-ignore:
      - 'README.md'
      - 'docs/**'
  pull_request:
    branches: [ main ]
    paths-ignore:
      - 'README.md'
      - 'docs/**'
  workflow_dispatch:
    inputs:
      enable_kernelsu:
        description: 'Enable KernelSU'
        required: false
        default: 'true'
        type: boolean
      enable_susfs:
        description: 'Enable SusFS'
        required: false
        default: 'true'
        type: boolean

env:
  KERNEL_REPO: "https://github.com/Arron6994/kernel_build_A12"
  KERNEL_BRANCH: "main"
  DEVICE_NAME: "zeus"
  ANDROID_VERSION: "12"

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 1
        
    - name: Setup build environment
      run: |
        sudo apt update
        sudo apt install -y \
          build-essential \
          git \
          curl \
          wget \
          zip \
          unzip \
          python3 \
          python3-pip \
          bc \
          bison \
          flex \
          libssl-dev \
          libelf-dev \
          cpio \
          ccache
          
    - name: Setup ccache
      uses: hendrikmuhs/ccache-action@v1.2
      with:
        key: zeus-kernel-build
        max-size: 10G
        
    - name: Make scripts executable
      run: |
        chmod +x build.sh
        chmod +x scripts/setup.sh
        
    - name: Setup kernel source
      run: |
        echo "Cloning kernel source..."
        if [ ! -d "kernel" ]; then
          git clone --depth=1 -b ${{ env.KERNEL_BRANCH }} ${{ env.KERNEL_REPO }} kernel
        fi
        
    - name: Setup toolchain
      run: |
        echo "Setting up toolchain..."
        mkdir -p toolchain
        
        if [ ! -d "toolchain/clang" ]; then
          cd toolchain
          git clone --depth=1 https://gitlab.com/vermouth/android_prebuilts_clang_host_linux-x86_clang-r536225.git clang
          cd ..
        fi
        
        if [ ! -d "toolchain/gcc" ]; then
          cd toolchain  
          git clone --depth=1 https://gitlab.com/vermouth/android_prebuilts_clang_host_linux-x86_clang-r536225.git gcc
          cd ..
        fi
        
    - name: Setup KernelSU
      if: ${{ github.event.inputs.enable_kernelsu != 'false' }}
      run: |
        echo "Setting up KernelSU-Next..."
        if [ ! -d "kernel/KernelSU" ]; then
          cd kernel
          git clone --depth=1 -b next https://github.com/rifsxd/KernelSU-Next KernelSU
          cd ..
        fi
        
    - name: Setup SusFS
      if: ${{ github.event.inputs.enable_susfs != 'false' }}
      run: |
        echo "Setting up SusFS..."
        if [ ! -d "kernel/SusFS" ]; then
          cd kernel
          git clone --depth=1 https://github.com/sidex15/SusFS4KernelSU SusFS
          cd ..
        fi
        
    - name: Run build setup
      run: |
        ./build.sh --setup-only
        
    - name: Upload build artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: zeus-kernel-build-${{ github.run_number }}
        path: |
          out/*.zip
          out/build.log
          out/arch/arm64/boot/Image.gz
        retention-days: 30
